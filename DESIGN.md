# Design

## Direction

粉白软陶个人工作台。它沿用用户给出的胶囊导航、按压按钮和圆形 Kitty 舞台，但页面结构改为多视图，不做单页长滚动。

## Color

- 页面：淡紫白 `#f6f3ff`，局部使用冷蓝和薄荷色平衡粉色。
- 文字：深紫灰 `#4f4361`，正文 `#746983`。
- 动作：珊瑚粉 `#f47fa2`；选中状态可使用浅紫 `#ece5ff`。
- 状态：薄荷绿表示在用，琥珀色表示进行中。

## Type

中文正文使用项目内 Noto Sans SC，英文和数字使用 IBM Plex Sans。标题圆润、结实，但不使用渐变文字、全大写口号或夸张超大字号。

## Shape And Depth

- 顶部导航是唯一的大胶囊容器。
- 按钮有 3px 至 5px 的实体下沿，按下时回落。
- 内容区以无框分栏和独立项目卡片为主；卡片圆角 14px 至 16px，不嵌套卡片。
- Three.js 舞台使用多层圆盘、透明环和立体小物构成景深。

## Motion

- Kitty 默认上下浮动幅度明显，鼠标移动会带动人物和圆环产生不同速度的视差。
- 点击舞台触发一次更大的跳动。
- 页面切换使用短距离位移和裁切过渡；内容默认可见。
- `prefers-reduced-motion` 下停止循环动画，只保留状态切换。

## Responsive

- 1100px 以下压缩导航和双栏比例。
- 760px 以下顶部导航变为抽屉，首页人物舞台排在文案前。
- 390px 下按钮和导航触点不小于 44px，文本和 3D 画面不得横向溢出。
